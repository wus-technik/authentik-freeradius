server proxy-inner-tunnel {
    #
    #  This example is very simple.  All inner tunnel requests get
    #  proxied to another RADIUS server.
    #
    authorize {
        if (&User-Password) {
            update control { Proxy-To-Realm := "authentik" }
        }
    }

    post-proxy {
        eap
    }

    post-auth {
        eap
    }
}
